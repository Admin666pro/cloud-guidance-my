# Product Nav — 设计文档

## 气质与意象

**关键词**：纯净、通透、水晶质感、极简、未来感

**意象场景**：清晨第一缕阳光穿过凝结露珠的冰晶，折射出柔和的光晕。悬浮在空中的半透明玻璃面板，边缘泛着微光，像被打磨过的水晶原石。整个界面仿佛漂浮在深邃的太空背景中，每一块卡片都像精心雕琢的冰雕艺术品。

## 视觉策略

- **摄影/图像方向**：使用渐变背景代替图片，营造深邃空间感。深色太空蓝紫渐变作为基底，突出玻璃的通透感。
- **图形语言**：极简几何，大圆角（24px+），无边框设计，用阴影和光晕定义边界。图标使用 SF Symbols 风格的线性图标。

## 配色方案

| 颜色 | 用途 | 意象来源 |
|------|------|----------|
| `#0a0a1a` → `#1a0a2e` | 背景渐变 | 深邃夜空，水晶展台的基底 |
| `rgba(255,255,255,0.15)` | 玻璃卡片基底 | 磨砂玻璃，光线透过冰晶 |
| `rgba(255,255,255,0.08)` | 玻璃卡片 hover 态 | 光线折射增强 |
| `rgba(255,255,255,0.5)` | 玻璃边框 | 冰晶边缘的反光 |
| `#6366f1` | 主色调（蓝紫） | 极光般的光晕 |
| `#22d3ee` | 强调色（青蓝） | 水晶折射的冷光 |
| `#f472b6` | 点缀色（粉） | 朝霞映射在玻璃上的暖色 |

## 字体排版

- **主字体**：`Inter`（Google Fonts），`-apple-system` 作为 fallback，模拟 SF Pro 的视觉风格
- **字号层级**：`2.5rem` 标题 → `1.125rem` 副标题 → `0.938rem` 正文 → `0.75rem` 辅助
- **排版节奏**：宽松字距（`tracking-wide`），标题 `font-weight: 600`，正文 `font-weight: 400`，行高 `1.6`

## 动效与交互

- **运动性格**：缓慢、优雅、丝绸般顺滑
- **缓动曲线**：`cubic-bezier(0.4, 0, 0.2, 1)` — Apple 标准曲线
- **悬停效果**：卡片轻微上浮（`translateY(-4px)`）+ 光晕增强（`box-shadow` 扩散）
- **过渡时间**：`300ms` 悬停，`500ms` 页面入场
- **滚动**：平滑滚动，卡片入场时从底部淡入上浮

## 设计禁忌

- ❌ 不使用纯黑（`#000000`）
- ❌ 不使用直角边框
- ❌ 不使用粗重阴影（`box-shadow` 不透明度不超过 0.3）
- ❌ 不使用花哨的字体
- ❌ 不使用纯色卡片背景（必须保持半透明玻璃质感）
- ❌ 不使用强烈的对比色
- ❌ 避免信息密度过高，保持呼吸感